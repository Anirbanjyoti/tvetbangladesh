{
  "name": "@tvet/worker",
  "version": "1.0.0",
  "private": true,
  "description": "TVET Bangladesh Background Job Worker Service",
  "main": "./dist/worker.js",
  "scripts": {
    "dev": "tsx watch src/worker.ts",
    "build": "tsc",
    "start": "node dist/worker.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@tvet/logger": "*",
    "@tvet/types": "*",
    "@tvet/validation": "*",
    "dotenv": "^16.4.5",
    "pino": "^9.3.2"
  },
  "devDependencies": {
    "@tvet/config": "*",
    "@types/node": "^20.14.14",
    "tsx": "^4.16.5",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
